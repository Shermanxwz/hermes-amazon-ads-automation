{
  "package": "hermes-amazon-ads-automation",
  "version": "3.2.0",
  "scope": "Amazon advertising operations only",
  "runtime": {
    "control_plane": "Python 3.11+ standard library",
    "database": "SQLite WAL with periodic checkpoint, adaptive retention and safe reclaim",
    "hermes_contract": "hermes-agent 0.18.2+ with real tool, hook, command, skill and delegation contracts",
    "amazon_capability": "official Amazon Ads MCP plus public Advanced Tools contracts",
    "web": "authenticated responsive operations and exact payload-bound approval dashboard",
    "target_host": "2C2G Linux VPS"
  },
  "capabilities": [
    "exact live Hermes MCP tool and JSON Schema discovery",
    "deterministic sponsored-ads strategy engine",
    "payload-bound and expiring operator approval for high-risk structural plans",
    "campaign, ad-group, target, keyword, ad and portfolio plan execution after exact approval",
    "deterministic binding of real Amazon IDs into pre-approved dependent action templates",
    "permanently blocked billing, account administration, unknown, drifted, irreversible delete and black-box composite/bulk operations",
    "separate machine and optional restricted-command human approval credentials",
    "authenticated Web approval as the default human authority",
    "Hermes approval commands registered but mutation-disabled by default",
    "Hermes session lifecycle, LLM, tool and subagent hook integration",
    "automatic OBSERVE downgrade when Hermes reports a model fallback",
    "atomic executor reservations and cross-cycle cooldown",
    "independent read-only verifier in a different Hermes session",
    "structured partial, pending and failure outcome handling",
    "Marketing Stream dedupe and operational alerts",
    "recorded post-write evidence bound to verifier session and action ID",
    "expired, partial and unknown write quarantine without blind replay",
    "atomic SQLite backup and integrity verification",
    "historical and shadow deterministic replay CLI",
    "periodic adaptive retention, payload compaction, gzip snapshots and WAL reclaim",
    "disk-pressure circuit breaker and bounded durable outbox artifacts",
    "browser, stress, deployment, quality and branch-coverage CI gates"
  ],
  "production_acceptance_required": [
    "owner Amazon Ads OAuth and refresh recovery",
    "authenticated MCP initialize, tools/list and live schema synchronization",
    "visible profiles, manager relationships and permissions",
    "live report lifecycle, decompression and attribution backfills",
    "owner AWS Marketing Stream delivery",
    "historical replay plus sustained OBSERVE shadow review",
    "Amazon Ads Test Account or controlled real-account campaign-create canary with independent verifier readback",
    "authenticated Web approval on the deployed HTTPS origin",
    "Hermes command listing on every deployed interaction surface and restricted-command approval only where intentionally enabled",
    "actual VPS HTTPS, reboot, storage soak and backup-restore drill"
  ]
}
