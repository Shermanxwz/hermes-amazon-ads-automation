# Local-only template. Do not commit the populated file.
NEW_API_API_KEY=replace-locally
AMAZON_ADS_CLIENT_ID=replace-locally
AMAZON_ADS_CLIENT_SECRET=replace-locally
# Amazon OAuth access/refresh tokens are managed by Hermes under:
# $HERMES_HOME/mcp-tokens/amazon-ads.json
